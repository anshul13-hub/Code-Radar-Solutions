#include <stdio.h>
#include <math.h>
int main ()
{
    int radius;
    pi = 3.14;
    float Area;
    Area = pi * radius * radius ;
    scanf("%d",&radius);
    printf("Area:%.2f\n",Area);
    return 0;
}