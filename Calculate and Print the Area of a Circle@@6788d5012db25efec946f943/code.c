#include <stdio.h>
#include <math.h>
int main ()
{
    int radius;
    pi = 3.14;
    float Area;
    Area = pie * r * r ;
    scanf("%d",&radius);
    printf("Area:%.2f\n",Area);
    return 0;
}